## Notes
: Use `textContent` for inserting terminal output instead of `innerHTML`.

# FileSystem
- Used tree like structure : `Object for each directory` and an `Array for it's children (other directories or files)`.
- `Recursive` function with `for` loop -- Traversing through file system -- Add parent node to each directory == `cd ..`
- `map` - Array method for ls command.